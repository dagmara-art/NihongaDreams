{
  "name": "nihongadreams-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling and E2E test suite for nihongadreams.com (the site itself ships as static HTML/CSS/JS with no runtime deps).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
