// Single source of truth for cache-busting query strings on data fetches
// and dynamic imports. Bump when shipping data/JS that must invalidate
// browser caches on next page load.
export const ASSET_VERSION = 'lightbox-cta-live-1';
